#include <iostream>
#include <cstdio>
#include "person.h"
#include "inputoutput.cpp"
#include "BinarySearchTree.h"
using namespace std;

int main(int argc, const char * argv[]) {
	system("Color F0");
	//system("color %");
    inputoutput menu;
    menu.readFile("names.txt");
    int input = 0;
    while(input != 2) {
        cout << "\n*************************************************************************\n";
		cout << "*           Welcome to the Social Network                               *" << endl;
		cout << "*                                                                       *" << endl;
		cout << "*                                                                       *" << endl;
		string name;
        string password;
		cout << "*           Username: "; cin>>name; cin.ignore(1,'\n'); 
		cout << "*                                                                       *" << endl;
		cout << "*           Password: "; cin>>password;
		cout << "*                                                                       *" << endl;
		cout << "*                                                                       *" << endl;
		cout << "*************************************************************************" << endl;
		system("CLS");
        //string name;
        //string password;
        //cout << "Username: ";
        //cin.ignore(1,'\n');
        //getline(cin, name);
        //cout << "Password: ";
        //cin>>password;
        if(name != "admin")
            menu.userMenu(name, password);
        else
            menu.adminMenu(menu, password);
    
    }

	system("pause");
	return 0;
    
}
